import { createLayer } from 'phecda-vue'
import { ElDialog } from 'element-plus'
export const useModal = createLayer(ElDialog, { title: 'phecda' })
export const useModal2 = createLayer(ElDialog, { title: 'phecda' })
